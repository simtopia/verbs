name: test-build
run-name: ${{ github.actor }} test build
on: [pull_request]
jobs:
  rust_build:
    runs-on: ubuntu-latest
    container: rust:slim
    steps:
      - uses: actions/checkout@v2
      - run: cargo build
